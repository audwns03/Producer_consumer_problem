# Producer_consumer_problem

-Version1을 개선해서 데이터를 버리지 않고, 큐에 데이터가 없다면 대기하도록 만들었습니다.

<br>

*생산자 먼저 실행 시 버퍼가 가득찬 경우 생산자 무한대기를 들어간다-> 락을 가지고 있는 상태에서 sleep()을 들어감

*소비자 먼저 실행 시 버퍼가 비어있는 경우 소비자 무한대기를 들어간다-> 락을 가지고 있는 상태에서 sleep()을 들어감
