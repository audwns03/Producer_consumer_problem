# Producer_consumer_problem

-큐에 값을 넣고 받는 코드를 synchronized를 사용해 안전한 임계영역으로 만들었습니다. -> 여러 스레드에서 동시에 접근하는걸 제한하기 위해

<br>

*생산자를 먼저 실행한 경우 데이터를 받지 못하는 경우 생김(버퍼가 가득 찬 경우)

*소비자 스레드를 먼저 실행한 경우에도 데이터를 받지 못하는 경우 생김(버퍼가 빈 경우)
